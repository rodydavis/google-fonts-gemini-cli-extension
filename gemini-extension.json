{
  "name": "google-fonts",
  "version": "1.0.0",
  "contextFileName": "GEMINI.md",
  "mcpServers": {
    "google-fonts-local-mcp": {
      "command": "node",
      "args": ["${extensionPath}${/}scripts${/}server.js"],
      "cwd": "${extensionPath}"
    }
  }
}
